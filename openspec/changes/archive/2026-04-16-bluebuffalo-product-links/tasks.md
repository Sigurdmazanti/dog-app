## 1. Update bluebuffalo.yaml

- [x] 1.1 Populate `products.dry` with all provided dry dog food product URLs
- [x] 1.2 Populate `products.wet` with all provided wet dog food product URLs
- [x] 1.3 Populate `products.barf` with all provided fresh/raw product URLs
- [x] 1.4 Populate `products.misc` with all provided topper/supplement product URLs
- [x] 1.5 Populate `products.treats` with all provided treat product URLs
- [x] 1.6 Update `productCounts` totals: set each category count to the length of its list and set `total` to the sum of all categories

## 2. Verify

- [x] 2.1 Confirm `productCounts.total` equals the sum of all per-category counts in the YAML
- [x] 2.2 Confirm no product URLs are listed under `freeze-dried` (leave as `[]`)
