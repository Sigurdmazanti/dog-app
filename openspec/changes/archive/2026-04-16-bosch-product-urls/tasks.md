## 1. Update bosch.yaml

- [x] 1.1 Populate `products.misc` in `scraper/sources/bosch.yaml` with the 8 provided misc URLs
- [x] 1.2 Populate `products.treats` in `scraper/sources/bosch.yaml` with the 28 provided treat URLs
- [x] 1.3 Populate `products.wet` in `scraper/sources/bosch.yaml` with the 9 provided wet food URLs
- [x] 1.4 Populate `products.dry` in `scraper/sources/bosch.yaml` with the 64 provided dry food URLs (across all three provided batches)
- [x] 1.5 Set `productCounts.misc: 8`, `productCounts.treats: 28`, `productCounts.wet: 9`, `productCounts.dry: 64`, and `productCounts.total: 109` in `scraper/sources/bosch.yaml`
- [x] 1.6 Verify the YAML is valid and each list length matches its corresponding `productCounts` value
